tasks:
  - cephadm.shell:
      host.a:
        - ceph orch ls
        - |
          set -ex
          echo $hostname

  - exec:
      client.1:
      - |
        set -ex
        echo "<exec.client.1---- HOST/IP -- $(hostname)/$(hostname -I) ---->"